__pycache__/
*.pyc
cache/
.cache/
.coverage*

data
outputs
*.npy
*.ply

.*/
.env
build/

**.egg-info**
site


!.github

dist

Makefile

# Committed real-data fixtures for smoke tests (kept tiny on purpose)
!test/assets/**
# ...but never the bootstrap-generated sidecars: the fixtures stay bare so the
# smoke tests keep exercising the bootstrap path
test/assets/**/.apairo/

# Benchmark plots are generated on demand (`bench.py --plot`), not committed
benchmarks/*.png
# Local cache written by examples/join_persisted_prior.py (disposable)
examples/_cache/
# Scratch sequence staged by examples/rellis_camera_sync.py (disposable)
examples/_rellis_scratch/
